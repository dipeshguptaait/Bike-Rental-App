# Bike Rental App

This is a Vite + React bike rental app. Build with `npm run build` and preview with `npm run preview`.

Deploy: The repo includes a GitHub Actions workflow that builds and publishes `dist/` to GitHub Pages.